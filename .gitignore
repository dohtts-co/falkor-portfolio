# Dependencies
node_modules/

# Local environment secrets — never commit
.env

# Admin-uploaded images (stored on Cloudinary in production)
uploads/

# Database file — production data lives in Supabase, not in the repo.
# The committed data.json is the local dev copy and the seed for `npm run seed`.
# Uncomment to stop tracking it.
# data.json

# Vercel CLI
.vercel
